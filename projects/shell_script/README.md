This directory contains multiple scripts useful for benchmarking
- `create_and_flash.sh` -> for 2 parameters full benchmark
- `flash_image.sh` -> flash a single image
- `query_serial.py` -> launches the zephyr image and read its output
